# Server configuration
PORT=5174
# MongoDB connection string (example)
MONGO_URI=mongodb://localhost:27017/vehicle_docs
# JWT secret key (set securely in real .env)
JWT_SECRET=change_me_in_local_env
# File uploads directory
UPLOAD_DIR=uploads
# Optional: 32-byte base64 key for file encryption
FILE_ENCRYPTION_KEY=base64:GENERATE_AND_SET
# Payment configuration (stub)
PAYMENT_PROVIDER=demo
PAYMENT_BASE_FEE=100
